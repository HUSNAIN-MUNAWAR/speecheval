import { AppShell } from "@/components/app-shell";
import { PageHeader } from "@/components/ui";
export default function Page(){return <AppShell><PageHeader eyebrow="Quality gates" title="Regression Gates" description="Policy enforcement compares eligible runs only after the Benchmark Integrity Guard accepts the evidence." action="Policy API"/><section className="panel p-5"><p className="eyebrow">Policy builder contract</p><h2 className="mt-1 text-lg font-semibold">When a metric changes, enforce an explainable decision.</h2><div className="mt-5 grid gap-3 text-sm text-muted md:grid-cols-3"><div className="rounded-xl border border-line p-4">Metric, operator, threshold, severity</div><div className="rounded-xl border border-line p-4">Baseline, language/tag scope, minimum sample count</div><div className="rounded-xl border border-line p-4">Bootstrap interval, affected evidence, remediation</div></div><p className="mt-5 text-xs leading-5 text-muted">Create, test, and enforce policies through <span className="mono">/api/v1/regression-policies</span>. UI policy editing is deliberately API-backed rather than static mock data.</p></section></AppShell>;}
