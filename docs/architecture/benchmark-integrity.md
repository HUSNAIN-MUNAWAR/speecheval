# Benchmark Integrity Guard

A comparison is rejected when project, dataset version, dataset content hash, terminal state, or metric overlap is incompatible. Different execution profiles or frozen metric configuration produce `COMPARABLE_WITH_WARNINGS`, not a silently authoritative result. The UI and card export surface the state before a verdict.
