# Evaluation Pipeline

Phase 2 will execute the following pipeline: validate request → freeze manifest → enqueue → validate artifacts → execute available metrics → persist sample results → aggregate → compare a baseline → evaluate policies → generate exports → publish events.

A missing model dependency, reference audio file, or hardware capability must produce an explicit `unavailable`, `skipped`, or `failed` state. It may never produce a plausible numeric replacement.
