# Metric Plugin SDK

Every plugin declares ID, version, direction, inputs, hardware/dependency requirements, schemas, aggregation strategy, limitations, and citation. Every requested metric produces an explicit `SUCCESS`, `SKIPPED`, `UNAVAILABLE`, `FAILED`, `MOCK`, or `ESTIMATED` record.
