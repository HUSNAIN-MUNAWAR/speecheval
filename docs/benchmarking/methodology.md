# Benchmarking Methodology

A valid benchmark documents dataset content hash, sample inclusion rules, model release metadata, metric version/configuration, execution profile, normalization, exclusions, and comparison integrity. No universal quality score is produced by default.
