# Running on CPU

Use `local-cpu-lightweight` for PCM WAV fixtures on 4 GB RAM or more. It runs audio validation, duration, silence, clipping, estimated loudness, F0 proxy, text normalization, mock/manual WER/CER, and performance telemetry. It does not require a GPU, cloud credentials, Whisper, or SpeechBrain.
