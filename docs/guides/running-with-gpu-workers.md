# Running with GPU Workers

GPU profiles are optional capability declarations. The current CPU runtime does not require a GPU. Add GPU-only plugins only after worker capability validation, provenance capture, and explicit optional-dependency tests are in place.
