# Setting Regression Policies

Policies specify metric, direction/operator, threshold, baseline, minimum sample count, optional language/tag scope, severity, and remediation. They are blocked when the Integrity Guard rejects the comparison.
