# Statistical Comparison

SpeechEval uses paired bootstrap resampling on matched sample keys. A confidence interval entirely in the harmful direction is labeled likely regression; one entirely in the beneficial direction is likely improvement; an interval overlapping zero is no meaningful difference. Small matched sample counts are insufficient evidence.
